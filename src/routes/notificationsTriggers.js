const express = require('express')
const router = express.Router()
/*
const controller = require('../controllers/notificationsTriggers')

router.get('/', controller.getAll)

router.get('/:triggerId', controller.getById)

router.post('/', controller.create)

router.patch('/:triggerId', controller.update)

router.delete('/:triggerId', controller.delete)
*/
module.exports = router
